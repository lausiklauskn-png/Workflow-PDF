{
  "name": "workfloh-pdf",
  "version": "1.0.0",
  "private": true,
  "description": "Workflow PDF — Formulare einlesen, Felder setzen, als PDF ausgeben (PWA, ohne Build-Schritt)",
  "type": "module",
  "scripts": {
    "test": "node tests/syntax.mjs && node tests/e2e.mjs && node tests/uebersetzung.mjs && node tests/behoerde.mjs",
    "messen": "node tests/masse.mjs",
    "serve": "python3 -m http.server 8000"
  },
  "devDependencies": {
    "@pdf-lib/fontkit": "1.1.1",
    "pdf-lib": "1.17.1",
    "playwright-core": "1.56.1"
  }
}
